# Смешанная модель освоения слова (узнавание + воспроизведение)

Статус: baseline (отгружено) · 2026-06-09

## Цель

Каждое слово осваивается за **два навыка** — узнавание (MC, выбор) и
воспроизведение (Type, ручной ввод) — тренируемых **вперемешку** в случайном
порядке. Слово считается выученным, только когда набраны ОБА навыка.

## Изменения данных / API

Два счётчика в таблице `progress`: `mcCorrect` (верные выборы) и `typeCorrect`
(верные ручные вводы), оба `v.optional` (старые строки читаются как 0).
`recordAnswer` принимает `mode: "mc" | "type"` и при верном ответе (`quality>=1`)
растит соответствующий счётчик.

## Поведение

Пороги ([`src/lib/learning.ts`](../../src/lib/learning.ts)):
```
MC_TARGET   = 3   // верных выборов (узнавание)
TYPE_TARGET = 3   // верных ручных вводов (воспроизведение)
```
**Выучено = `mcCorrect>=MC_TARGET && typeCorrect>=TYPE_TARGET`.** Типы упражнений
(`ExerciseType`): `mc_pt_ru`, `mc_ru_pt`, `type_pt`.

## Ключевые решения и алгоритмы

**Пороги ДУБЛИРУЮТСЯ на сервере** (`isLearned` в [`convex/progress.ts`](../../convex/progress.ts))
и на клиенте (`isWordLearned` в [`learning.ts`](../../src/lib/learning.ts)) —
**держать синхронно**: Convex бандлится отдельно и не делит модули с `src/`.

**`pickExerciseType(card, tag, rnd)`** — тип следующего показа слова:
- Пока слово не выучено (нужен MC или Type):
  - **Первое знакомство** (`mc===0 && type===0`) → **всегда MC** (`mc_pt_ru`/`mc_ru_pt`
    50/50): ручной ввод по никогда не виденному слову набрать нельзя.
  - Дальше → случайно среди ещё НЕ набранных навыков (`pickType = !firstEncounter &&
    needType && (!needMc || rnd()<0.5)`). Так сессия гарантированно добирает и
    `MC_TARGET` выборов, и `TYPE_TARGET` вводов, но вперемешку.
- Выучено (повторение): на `due` — уклон в ручной ввод (50% `type_pt`, иначе MC);
  иначе случайный микс всех трёх. `rnd` инжектируется ради детерминизма в тестах.

**Классификация монотонна.** Счётчики только растут → слово не «разучивается».
Старые «выученные» строки уже имеют `mc>=3` (в прежней фазовой модели `type` рос
только после фазы выбора), поэтому правило «оба навыка» их не разучивает —
**миграция не нужна**.

## Тестирование

- [`src/lib/learning.test.ts`](../../src/lib/learning.test.ts): `pickExerciseType`
  (первое знакомство → MC; добор недостающих навыков; детерминизм через
  инжектируемый `rnd`), `isWordLearned`, `shouldRequeue`/`requeuePosition`
  (см. [`session-queue-and-rotation.md`](session-queue-and-rotation.md)).
- [`convex/progress.test.ts`](../../convex/progress.test.ts): рост `mcCorrect`/
  `typeCorrect` по `mode`, «выучено» по обоим навыкам.

## Карта файлов

- [`src/lib/learning.ts`](../../src/lib/learning.ts) — `MC_TARGET`/`TYPE_TARGET`,
  `isWordLearned`, `pickExerciseType`.
- [`convex/progress.ts`](../../convex/progress.ts) — `isLearned` (дубль порогов),
  `recordAnswer` (рост счётчиков по `mode`).
- [`src/lib/types.ts`](../../src/lib/types.ts) — `ExerciseType`, `CardFields`, `BadgeTag`.

## Известные ограничения

- Большой урок (до 10 слов) на первой тренировке длинный: каждое слово доводится
  до «выучено» за сессию (~`MC_TARGET`+`TYPE_TARGET` верных ответов на слово).
- Пороги дублированы (сервер/клиент) — рассинхрон порогов был бы тихим багом.
