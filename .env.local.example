# Copy to .env.local and fill with your Convex DEV deployment URL.
# `npx convex dev` prints it and also writes it to .env.local automatically.
VITE_CONVEX_URL=https://your-dev-deployment.convex.cloud
