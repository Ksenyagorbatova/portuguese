// Registers jest-dom matchers (toBeInTheDocument, toHaveTextContent, …) on
// Vitest's `expect`, and augments the matcher types. Loaded via
// test.setupFiles for the frontend project (see vitest.config.ts).
import "@testing-library/jest-dom/vitest";
